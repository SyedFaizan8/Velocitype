{
  "name": "@repo/db",
  "version": "1.0.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:studio": "prisma studio",
    "db:generate": "prisma generate",
    "db:push": "prisma push db",
    "db:migrate": "prisma migrate dev --skip-generate"
  },
  "devDependencies": {
    "prisma": "^6.3.1"
  },
  "dependencies": {
    "@prisma/client": "^6.3.1"
  }
}
